
import React from 'react';
import RoutesPage from './RoutesPage';

// Simple redirect component to maintain the existing route structure
const RoutesList = () => {
  return <RoutesPage />;
};

export default RoutesList;
